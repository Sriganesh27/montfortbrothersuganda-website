<?php

class DeletestudentModel
{
    // Original File: api/students/deletestudent.php


    // Extracted SQL references from original code
    // // 2. Delete Dependents
    // $stmt = $conn->prepare("DELETE FROM $tbl WHERE AdmissionNo = ? AND branch_id = ?");
    // // 3. Delete Student
    // $stmt_del = $conn->prepare("DELETE FROM erp_students WHERE AdmissionNo = ? AND branch_id = ?");

}
?>